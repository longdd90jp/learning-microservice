spring:
  application:
    name: service-uaa
  config:
    import: "configserver:"
  cloud:
    config:
      uri: http://localhost:9000
      username: root
      password: s3cr3t
      name: global, service-uaa
      profile: default